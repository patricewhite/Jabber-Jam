

const users = {
  create:function(user){
    console.log("hey");
    const field = {
      username: user,
      // password: user.password,
      // firstName: user.firstName,
      // lastName: user.lastName,
      // email: user.email
    };
    this.fields.push(post);
    return field;
  },
  // get: null,
  // put:null,
  // delete: null
}

module.exports = {Users: users};
